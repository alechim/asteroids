# asteroids

This is my second boot.dev project. This was programmed in Python and uses the PyGame library.

## About

## Usage

---

*Use `python3` instead of `python` on Unix-based systems (Linux/macOS/WSL) if needed.*
